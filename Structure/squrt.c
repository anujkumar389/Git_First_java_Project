#include <math.h>
#include<stdio.h>

// double sqrt(double num) {
//     return sqrt(num);
// }
int main(){
    int a=2;
    int b=pow(a,3);
    printf("%d\n",b);
    // printf("%d",pow(b, 2));
    return 0;
}